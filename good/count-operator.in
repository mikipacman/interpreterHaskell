int compare_3() {
	bool a = 1 < 3;
	bool b = 2 < 2;
	bool c = 3 < 1;

	return 0;
}

int main() {
	int comp;
	printInt(comp);

	count < cost 2 with comp;

	compare_3();

	printInt(comp);
	printInt(get cost <);
	return 0;
}